@using ZenBoxUI.Blazor.Components.Snippet

<div>
  <h1>Bind Text</h1>

  <p class="my-4">
    The <span class="zb-code">@("<")ZbInputNumber@(">")</span>, can be bound to anu numeric type by using <span class="zb-code">@@bind-Value</span>
  </p>

  <ZbSnippet>
    <ZbSnippetContent>
      <ZbNumberInput CssClass="zb-demo-width" @bind-Value="@_value" />
      <p class="pt-1 pb-2">Value: @(_value == null ? "Null" : _value)</p>
    
      <ul>
        <li><span class="zb-code">@@bind-Value</span> - Binds text to a variable</li>
      </ul>
    </ZbSnippetContent>
    <ZbSnippetFile Name="Example.razor">
      <CodeHighlight Code="@_codeExample" CodeLanguage="razor" />
    </ZbSnippetFile>
  </ZbSnippet>
</div>


@code {

  private int? _value { get; set; } = 0;
  private string _codeExample = null!;

  protected override void OnInitialized()
  {
    var path = Path.Combine(Directory.GetCurrentDirectory(),
      "Components", "Pages", "Components", 
      "InputNumber", "CodeSnippets", "InputNumberBind.razor.txt"
      );
    _codeExample = File.ReadAllText(path).Replace("\r\n", "\n");
  }
}
